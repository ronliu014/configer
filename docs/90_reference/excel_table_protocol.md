# Excel 配表协议

状态：Active

本文记录 `configer` 读取 DD 策划配表时必须遵守的 Excel 结构协议。具体字段含义仍以各模块的游戏配置说明为准。

## 4 行表头协议

配表工作表前 4 行是元信息，第 5 行开始才是业务数据。

| 行号 | 含义 | 工具用途 |
|---|---|---|
| 第 1 行 | 字段中文名 | UI 显示、变更预览、changelog |
| 第 2 行 | 导出标记 | 判断字段导出去向和部分字段性质 |
| 第 3 行 | 数据类型 | 初步类型解析和输入校验 |
| 第 4 行 | 字段英文 key | 源表字段名，保存为 `srcName` |

工具解析时必须同时记录 sheet 名、列坐标、源字段名和源行号。业务逻辑不得直接依赖 `Remark10` 这类源字段名，应使用模块 schema 中定义的稳定逻辑 key。

## 导出标记

装备表中已确认的标记含义如下：

| 标记 | 含义 | 说明 |
|---|---|---|
| `A` | 双端导出 | 客户端和服务端都使用 |
| `S` | 仅服务端 | 服务端配置字段 |
| `C` | 仅客户端 | 客户端展示字段 |
| `N` | 不导出 | Excel 内部草稿列或推导列 |

不同模块如有差异，必须在 `docs/40_game_config/<module>/` 中补充说明。工具不得只根据导出标记判断是否可写，是否可写必须以 schema 的字段来源和 `editable` 规则为准。

## 字段来源与写回

| 来源 | 说明 | v1.0 写回 |
|---|---|---|
| `manual` | 策划手填字段 | 允许 |
| `formula` | Excel 公式生成字段 | 禁止 |
| `ref` | 关联字段或引用字段 | 默认禁止，除非 schema 明确标为手填引用 |
| `imported` | 外部表导入或公式拉取字段 | 禁止 |
| `hidden` / `deprecated` | 隐藏或废弃字段 | 禁止 |

回写只能修改 schema 明确允许的字段。公式列、自动列、导入列和只读关联字段不得写成静态值。

## workbook 保护规则

Excel 写回必须基于加载时的原 workbook 逐单元格修改。禁止使用整表重建方式覆盖文件。

必须保护：

- 公式。
- 单元格格式。
- 批注。
- 合并单元格。
- 未修改的 sheet。
- 未修改的行列。

新增行时，只填允许写回的手填字段。公式列应沿用原表公式策略，具体实现需要在回写测试中验证。

## 解析失败处理

以下情况必须阻止相关表进入可写状态：

- 少于 4 行表头。
- 必要字段缺失。
- schema 声明字段无法映射到源列。
- 主键字段为空或重复。
- 字段类型无法解析且影响写回安全。

错误信息必须包含文件、sheet、行、列和原因，方便策划或工具维护者定位。

## 参考来源

- `docs/40_game_config/equip/01_表结构与导出规则.md`
- `docs/40_game_config/equip/12_装备配置工具设计规范.md`
- `docs/20_architecture/data_lifecycle.md`
- `docs/30_development/coding_standard.md`
