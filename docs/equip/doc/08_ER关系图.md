# 08 · equip 装备配置链 · ER 关系图

整条装备数据链共 **17 张配置表 = 活跃 16 + 废弃 1**（活跃含与 equip 配套的道具表 item、属性主表 prop、技能表 skill、装备技能随机库 equip_skill_drop + equip_skill_effect；废弃 1 = equip_random_proplib_skill，已被 equip_skill_drop/effect 取代、**不计入活跃表数**；[09](09_关联表字段清单.md) 目录按活跃 16 张列），另有通用语言表 language 被多表引用。下面用 Mermaid ER 图表示它们之间的主键(PK)/外键(FK)关联。

> 在支持 Mermaid 的编辑器（飞书文档、Typora、VS Code 预览、GitHub）里可直接渲染。

---

## 完整 ER 图

```mermaid
erDiagram
    EQUIP }o--|| JOB_GROUP : "UseJobRequest = GroupID"
    EQUIP }o--|| PROPLIB : "PropLibID"
    EQUIP }o--|| RANDOM_PROPLIB : "FixedPropLibID = 库ID"
    EQUIP }o--o| SPECIAL_DROP : "DropID(可空)"
    EQUIP }o--o| SUIT : "SuitId(可空)"
    EQUIP }o--|| EQUIP_RARE : "品质→道具品质/强度子品质(外链)"
    EQUIP }o--o{ SKILL_DROP : "RandonSkillID = SkillGroup(可空,可多值)"

    JOB_GROUP }o--o{ SOUL : "JobID 含多个 SoulID"
    SOUL ||--o{ SOUL : "ParentId 职业树"

    RANDOM_PROPLIB }o--|| RANDOM_PROPLIB_GROUP : "按品质取乘算比例"

    SPECIAL_DROP }o--|| SPECIAL_PROP : "PropID"
    SPECIAL_PROP }o--o| SPECIAL_EXPROP : "额外生效 PropID(当前空)"

    EQUIP ||--|| ITEM : "配套: 装备道具ItemId = EquipID"
    ITEM |o--o| ITEM : "非绑↔绑定 配对(BindItemId/UnBindItemId)"

    PROPLIB }o--|| PROP : "属性值的属性ID → 属性编号"
    RANDOM_PROPLIB }o--|| PROP : "属性id → 属性编号"
    SPECIAL_EXPROP }o--|| PROP : "额外生效属性ID → 属性编号"
    SPECIAL_PROP }o--|| SKILL : "特殊效果 → 技能id"
    SKILL_DROP }o--o{ SKILL_EFFECT : "EquipSkillEffectID"
    SKILL_EFFECT }o--|| SKILL : "EquipSkillId → 技能id"
    SKILL_EFFECT }o--o| PROP : "属性值ID Prop → 属性编号(可空)"
    SOUL }o--o{ SKILL : "初始技能/闪避/普攻 → 技能id"
    SKILL |o--o{ SKILL : "关联技能ID 自关联"

    EQUIP {
        string EquipID PK "装备ID"
        int    UseJobRequest FK "使用职业限制"
        int    EquipType "装备部位(手填)"
        int    TransferNum "转数(手填)"
        int    RealLevel "真实等级"
        int64  PropLibID FK "初始基础属性库id"
        string FixedPropLibID FK "初始随机属性库id"
        int    DropID FK "特殊属性库ID"
        int    SuitId FK "初始套装库"
    }

    JOB_GROUP {
        int    GroupID PK "组ID"
        string JobID FK "职业ID列表(逗号分隔)"
        string BaseJob "基础职业"
    }

    SOUL {
        int    SoulID PK "职业ID"
        string Name "职业名称"
        int    BasicsJob "基础职业"
        int    TransferStage "转职阶段"
        int    BranchJob "职业分支"
        int    ParentId FK "父职业"
    }

    PROPLIB {
        int64  PropLibID PK "属性库ID"
        string Props "属性值(属性ID,数值)"
    }

    RANDOM_PROPLIB {
        int    SID PK "流水ID"
        int64  RandomPropLib "随机库ID"
        string PropValueID "属性id"
        int    PropNum "基准值"
        int    Weight "权重"
    }

    RANDOM_PROPLIB_GROUP {
        int    ID PK "流水ID"
        int    EquipRare "装备品质"
        string RandomInterval "随机比例区间"
        int    Weight "概率"
    }

    SPECIAL_DROP {
        int    ID PK "流水ID"
        int    DropID "属性库ID"
        int    PropID FK "属性ID"
        int    Weight "权重"
    }

    SPECIAL_PROP {
        int    PropID PK "特殊效果ID"
        int    Type "效果类型"
        string Prop "技能ID"
        int    CPValue "评分"
    }

    SPECIAL_EXPROP {
        int    PropID PK "属性ID"
        int    FuncID "玩法ID"
        string ExtraProp "额外生效属性"
    }

    SUIT {
        int    SuitId PK "套装编号"
        string SuitName "套装名称"
        int    SuitSum "套装总数"
    }

    EQUIP_RARE {
        int    EquipRare PK "装备品质"
        int    Rare "道具品质"
        int    StrengthRare "强度子品质"
        string EquipSwallow "进阶可吞噬品质"
    }

    ITEM {
        string ItemId PK "道具ID(装备道具=装备ID)"
        string BindItemId FK "绑定物品ID(非绑填)"
        string UnBindItemId FK "未绑定物品ID(绑定填)"
        int    Rare "道具品质"
        string SellItems "出售返还(道具ID,数量)"
    }

    PROP {
        int    Id PK "属性编号"
        string PropName "属性名称(语言包)"
        int    PropType "属性类型"
    }

    SKILL {
        string Id PK "技能id"
        string ModelId "技能逻辑ID"
        string CallSkillList FK "关联技能ID(自关联)"
    }

    SKILL_DROP {
        int    ID PK "本表主键(行ID)"
        int    SkillGroup "技能组ID(被equip字段47引用)"
        string EquipTypeList "装备部位列表"
        string JobIDList FK "职业要求(→SoulID)"
        int    Weight "权重"
        int    EquipSkillEffectID FK "关联技能效果编号"
    }

    SKILL_EFFECT {
        int    SID PK "流水id"
        int    EquipSkillEffectID "装备技能效果ID(被drop引用,分组)"
        string EquipSkillId FK "技能ID→skill"
        int    EquipSkillSuitNum "套装件数触发"
        string Prop FK "属性值ID→属性编号"
        int    CombatValue "评分"
        int    EquipSkilllevel "技能等级"
    }
```

---

## 关系速读

| 关系 | 类型 | 含义 |
|------|------|------|
| EQUIP → JOB_GROUP | 多对一 | 多件装备共用一个职业组 |
| EQUIP → PROPLIB | 多对一 | 装备指向一条固定属性 |
| EQUIP → RANDOM_PROPLIB | 多对一 | 装备指向一个随机词条库 |
| EQUIP → SPECIAL_DROP | 多对一(可空) | 仅带"特效"的装备才有 |
| EQUIP → SUIT | 多对一(可空) | 仅套装装备才有 |
| EQUIP → EQUIP_RARE | 多对一 | 按品质查道具品质/强度子品质（外部链接，N列草稿用） |
| JOB_GROUP ↔ SOUL | 多对多 | 一个组含多个职业，一个职业可在多组 |
| SOUL → SOUL | 自关联 | ParentId 构成职业树（基础→转职→分支） |
| RANDOM_PROPLIB → RANDOM_PROPLIB_GROUP | 多对一 | 按装备品质取乘算比例与概率 |
| SPECIAL_DROP → SPECIAL_PROP | 多对一 | 特效库条目指向具体效果 |
| SPECIAL_PROP → SPECIAL_EXPROP | 多对一(可空) | 玩法/位面限定的额外属性（当前空表） |
| EQUIP ↔ ITEM | 一对一 | 装备道具的 ItemId = 装备ID，每件装备配套一条道具 |
| ITEM ↔ ITEM | 一对一(配对) | 非绑道具与绑定道具互指（绑定版="8"+ID），靠 BindItemId/UnBindItemId |
| PROPLIB / RANDOM_PROPLIB / SPECIAL_EXPROP → PROP | 多对一 | 各表的"属性ID"指向属性主表 prop 的属性编号 |
| SPECIAL_PROP → SKILL | 多对一 | 特效的"特殊效果"指向技能表 skill |
| EQUIP → SKILL_DROP | 多对一(可空,可多值) | 字段47「装备技能随机库」，值=技能组ID(可多个分号分隔)，按 SkillGroup 分组取候选技能 |
| SKILL_DROP → SKILL_EFFECT | 多对多 | 每条候选技能经 EquipSkillEffectID 指向具体效果配置 |
| SKILL_EFFECT → SKILL | 多对一 | 效果的 EquipSkillId 指向技能表 skill |
| SKILL_EFFECT → PROP | 多对一(可空) | 效果附带固定属性的"属性ID"指向属性主表 prop |
| SOUL → SKILL | 多对多 | 职业的初始技能/闪避/普攻指向技能表 skill |
| SKILL → SKILL | 自关联 | 关联技能ID 引用其它技能 |

---

## 简化版（只看主表对外的直连）

给只想抓主干的人——equip 主表直接连出去的表：

```mermaid
flowchart LR
    EQUIP["⭐ equip 装备主表"]
    EQUIP -->|UseJobRequest| JG["equip_job_group 职业组"]
    EQUIP -->|PropLibID| PL["equip_proplib 固定属性"]
    EQUIP -->|FixedPropLibID| RP["equip_random_proplib 随机词条"]
    EQUIP -->|DropID| SD["equip_special_drop 特效"]
    EQUIP -->|SuitId| SU["equip_suit 套装"]
    EQUIP -->|品质| RARE["equip_rare 稀有度/品质"]
    EQUIP -.->|EquipID=ItemId 配套| ITEM["item 道具表(非绑+绑定)"]

    JG -->|JobID| SOUL["soul 职业总表"]
    RP -->|品质乘算| RPG["equip_random_proplib_group"]
    RP -->|属性id| PROP["prop 属性主表"]
    PL -->|属性ID| PROP
    SD -->|PropID| SP["equip_special_prop 特效效果"]
    SP -->|额外生效| SEP["equip_special_exprop 玩法额外属性"]
    SP -->|特殊效果| SKILL["skill 技能表"]
    SOUL -->|技能字段| SKILL
    EQUIP -->|RandonSkillID 字段47| SKD["equip_skill_drop 技能随机库"]
    SKD -->|EquipSkillEffectID| SKE["equip_skill_effect 技能效果"]
    SKE -->|EquipSkillId| SKILL
    SKE -.->|属性值ID| PROP
    ITEM -.->|绑定↔非绑 互指| ITEM
```

> equip 与 item 是**配套**关系（虚线）：每件装备在 item 表里配一条非绑道具 + 一条绑定道具（ID 差"8"前缀），装备道具的 ItemId 就是装备的 EquipID。

---

## 表清单（活跃 16 + 废弃 1 = 17 张配置表 + 通用语言表）

| # | 表 | 角色 | 详见 |
|---|----|----|------|
| 1 | equip | 装备主表（清单+指针） | [02](02_字段字典与公式拆解.md) / [05](05_字段清单表.md) |
| 2 | equip_job_group | 职业组（谁能穿） | [06](06_关联配置表说明.md) |
| 3 | equip_proplib | 固定基础属性 | [06](06_关联配置表说明.md) |
| 4 | equip_random_proplib | 随机词条池（含1000x技能段） | [06](06_关联配置表说明.md) |
| 5 | equip_special_drop | 特效库 | [06](06_关联配置表说明.md) |
| 6 | equip_suit | 套装 | [06](06_关联配置表说明.md) |
| 7 | equip_random_proplib_group | 随机词条品质乘算/概率 | [07](07_深层依赖表说明.md) |
| 8 | equip_special_prop | 特效具体效果(技能) | [07](07_深层依赖表说明.md) |
| 9 | soul | 职业总表/职业树根表 | [07](07_深层依赖表说明.md) |
| 10 | equip_special_exprop | 玩法/位面限定额外属性(当前空) | [07](07_深层依赖表说明.md) |
| 11 | equip_rare | 装备稀有度/品质映射 | [07](07_深层依赖表说明.md) |
| 12 | equip_random_proplib_skill | 技能随机库（⚠️ 废弃冗余，已被 equip_skill_drop/effect 取代） | [07](07_深层依赖表说明.md) |
| 13 | item | 道具表（与 equip 配套，含非绑/绑定） | [09](09_关联表字段清单.md) |
| 14 | prop | 属性主表（属性名↔属性编号） | [09](09_关联表字段清单.md) |
| 15 | skill | 技能表（特效/职业技能） | [09](09_关联表字段清单.md) |
| 16 | equip_skill_drop | 装备技能随机库（字段47，按技能组分组、走权重） | [09](09_关联表字段清单.md) |
| 17 | equip_skill_effect | 装备技能效果（技能/套装件数/属性/评分/转变外观） | [09](09_关联表字段清单.md) |
| — | language | 通用语言表（被多表语言包key引用） | [09](09_关联表字段清单.md) |
